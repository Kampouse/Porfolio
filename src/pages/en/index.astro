---
import { changeLanguage, t } from "i18next";
import BaseLayout from "../../layouts/BaseLayout.astro";
import HorizontalCard from "../../components/HorizontalCard.astro";
import { getCollection } from "astro:content";
import { LanguageSelector } from "astro-i18next/components";

changeLanguage("en");

const getCurrentLanguage = () => {
    //get current language from url
    const url = window.location.href;
    const lang = url.split("/")[3];
    return "/" + lang;
};
const posts = (await getCollection("blog")).sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
const last_posts = posts.slice(0, 3);
---

<BaseLayout>

   <LanguageSelector
  showFlag={true}
  languageMapping={{ en: "english", fr: "french", es : "spanish" }}
  class="my-select-class"

/>

    <div class="pb-12 mt-5">
      <div class="text-xl py-1">{t("home.hi-there","en")}</div>
      <div class="text-5xl font-bold">{t("home.i-am")} </div>
      <div class="text-3xl py-3 font-bold">{t("home.my-title")}</div>
      <div class="py-2">
        <text class="text-lg">
          {t("home.i-do")}
          </text
        >
      </div>
      <div class="mt-8">
        <a class=" btn btn-outline bg-info" href="https://www.linkedin.com/in/jemartel/" target="_blank"> Let's connect!</a>
      </div>
    </div>

    <div>
      <div class="text-3xl w-full font-bold mb-2">{t("home.latest-project")}</div>
    </div>

    <HorizontalCard
      title="Transcendence"
      img="/post_img.webp"
      desc="Final project for the 42 school; full stack web application that lets you play with other people  in real time to pong Made with React, Node.js, Express, Socket.io, and PostgreSQL. and of course of Typescript."
      url="/projects/transcendence"
      showcase="httpss://youtu.be/4Z4Y4Z4Y4Z4"
      target="_self"
      repo="https://github.com/Kampouse/webserv/"
    />
    <div class="divider my-0"></div>
    <HorizontalCard
    title="Webserver"
    img="/post_img.webp"
    desc="ever wonder how  http request are  processed? well this project is a simple implementation of  a webserver in C++(98)"
    url="/projects/webserv"
    target = "_self"
    showcase="httpss://youtu.be/4Z4Y4Z4Y4Z4"
    repo="https://github.com/Kampouse/webserv/"
  />
    <div class="divider my-0"></div>
    <HorizontalCard
    title="Cube3D"
    img="/post_img.webp"
    desc=" A simple raycaster  made with C and the MLX42 library; tlrd:2d map to 3d view."
    target="_self"
    repo="https://github.com/Kampouse/webserv/"
    url="/projects/cube3d"
    showcase="httpss://youtu.be/4Z4Y4Z4Y4Z4"
  />
    <div>
     <!-- <div class="text-3xl w-full font-bold mb-5 mt-10">Latest from blog</div>  !-->
    </div>

    {
      /*
      last_posts.map((post) => (
        <HorizontalCard
          title={post.data.title}
          img={post.data.heroImage}
          desc={post.data.description}
          url={"/blog/" + post.slug}
          target="_self"
          badge={post.data.badge}
        />
        <div class="divider my-0"></div>
      ))
      */
    }

</BaseLayout>
